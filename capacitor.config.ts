import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DummyProject',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
